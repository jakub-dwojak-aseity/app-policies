# Archiwum — historia, nie źródło prawdy

Miejsce na przekazania i migawki stanu witryny z sesji **zamkniętych**. **Dziś jest puste**
i to nie jest przeoczenie: tor witrynowy nigdy nie założył tu datowanego dokumentu — pomiary
SEO/AEO od początku szły do `jp-grammar/docs/PLAN_SEO_AEO.md`, a przekazania do hubu
(`jp-grammar/docs/archiwum/PRZEKAZANIE_09_09_SEO_WIECZOR.md`).

**Nie czytaj stąd stanu.** Wejście robocze to `../TERAZ.md`, mapa terenu to `../../AGENTS.md`,
mapa rodziny to `/Users/jakub/aseity/jp-grammar/AGENTS.md`. `../DOMENA.md` **zostaje w `docs/`**,
bo jest opisem dziesięciu kroków z pomiarem pod każdym, a nie migawką sesji — ma za to nagłówek
mówiący, że kroki są już wykonane.

Wchodzi się tu tylko po jedno: **dlaczego** coś kiedyś rozstrzygnięto tak, a nie inaczej — i to
wtedy, gdy `git log` nie wystarczy. Jeśli znajdziesz tu regułę, która wciąż obowiązuje, to znaczy,
że nie została osuszona na czas: przenieś ją do `../TERAZ.md` albo do
`jp-grammar/docs/FAMILY_CONVENTIONS.md`, gdy obowiązuje siostry.

Nic się tu nie dopisuje.
